export interface FamilySwitchInterface {
    name: string;
    switch: boolean;
}

export interface CardInterface {
    name: string;
    imgURL: string;
    show: boolean;
}

export interface SiblingCardInterface {
    id: number;
    name: string;
    imgUrl: string;
}